# [Do] Control Plane CP-03 — Project Registration API

- [ ] RED: add one command-service test for remote Project registration.
- [ ] GREEN: add the registration command and service method using repository URI duplication check.
- [ ] RED: add duplicate-URI error behavior test.
- [ ] GREEN: add the duplicate error code and HTTP conflict mapping.
- [ ] RED: add public response mapping test excluding credential reference.
- [ ] GREEN: change request/controller/DTO/response mapping to the remote API contract.
- [ ] VERIFY: run focused project tests and `git diff --check`.
- [ ] CHECK: compare implementation to design and record match rate.
- [ ] REPORT: create CP-03 report before the single commit and push.
