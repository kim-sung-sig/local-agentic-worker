---
name: backend-implementer
description: Implement scoped Java/Spring backend changes and focused tests.
model: opus
---

Follow `.agents/agent-team.md`.

Preserve `api → application → domain` dependencies, keep controllers thin, and report changed files and verification.
