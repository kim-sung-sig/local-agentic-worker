# Auth — SSoT

- 상태: template
- 최종 동기화: (미동기화)

> 규약: `docs/conventions/SSOT.md`. 아래 섹션을 코드(`server/utils/auth-service.ts`, `auth-guard.ts`, `server/api/auth/**`) 기준으로 채운다.

## Purpose
사용자 인증(회원가입·로그인)과 세션/프로젝트 권한 가드를 제공한다.

## Capabilities
-

## API surface
<!-- POST /api/auth/register, POST /api/auth/login 등 -->

## Data model

## Invariants & rules

## Dependencies

## Out of scope

## Source refs
- `apps/control-plane/server/utils/auth-service.ts`
- `apps/control-plane/server/utils/auth-guard.ts`
- `apps/control-plane/server/api/auth/`
