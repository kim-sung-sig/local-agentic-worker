[project]
name = "python-agent-worker"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["fastapi>=0.115", "psycopg[binary]>=3.2", "uvicorn>=0.30"]

[dependency-groups]
dev = ["pytest>=8.0", "httpx>=0.27"]

[tool.pytest.ini_options]
pythonpath = ["src"]
